# Copia este archivo a `.env` y completa con tu cadena de conexión.
# Destino de despliegue: Supabase (PostgreSQL).
# Formato Supabase (transaction pooler, puerto 6543): postgresql://postgres.<ref>:<password>@<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require
# Reemplaza <TU_PASSWORD> por la contraseña real de la base.
DATABASE_URL="postgresql://postgres.zckalzupbidxsssjajzl:<TU_PASSWORD>@aws-0-us-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require"
